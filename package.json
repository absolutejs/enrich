{
  "author": "Alex Kahn",
  "description": "In-house B2B email verification + discovery — MX/SMTP/catch-all verification and decision-maker email finding, so you don't pay PDL/Apollo per lookup.",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "5.8.3"
  },
  "license": "BSL-1.1",
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "name": "@absolutejs/enrich",
  "repository": {
    "type": "git",
    "url": "https://github.com/absolutejs/enrich.git"
  },
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts --outdir dist --target=bun && tsc --emitDeclarationOnly --project tsconfig.json",
    "release": "bun run build && bun publish",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "types": "./dist/index.d.ts",
  "version": "0.0.4",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}
